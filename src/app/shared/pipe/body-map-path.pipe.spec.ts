import { BodyMapPathPipe } from './body-map-path.pipe';

describe('BodyMapPathPipe', () => {
  it('create an instance', () => {
    const pipe = new BodyMapPathPipe();
    expect(pipe).toBeTruthy();
  });
});
