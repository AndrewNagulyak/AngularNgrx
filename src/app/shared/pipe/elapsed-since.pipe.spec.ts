import { ElapsedSincePipe } from './elapsed-since.pipe';

describe('ElapsedSincePipe', () => {
  it('create an instance', () => {
    const pipe = new ElapsedSincePipe();
    expect(pipe).toBeTruthy();
  });
});
