import { ModernDatePipe } from './modern-date.pipe';

describe('ModernDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ModernDatePipe();
    expect(pipe).toBeTruthy();
  });
});
