export enum ApiRoutes {
  tasks = '/tasks',
  boards = '/api/boards',
  cards = '/cards',
  logOut = '/auth/logout',
  profile = '/users/profile',
  logIn = '/auth/signin',
  lastBoards = '/api/boards/last',
  signup = '/auth/signup'

}
