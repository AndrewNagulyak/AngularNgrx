import * as Home.ActionActions from './home.action.actions';

describe('Home.Action', () => {
  it('should create an instance', () => {
    expect(new Home.ActionActions.LoadHome.Actions()).toBeTruthy();
  });
});
